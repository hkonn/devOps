package com.pgr301.exam;
/*
import com.pgr301.exam.model.Account;
import com.pgr301.exam.model.Transaction;
import org.junit.jupiter.api.Test;

import java.math.BigDecimal;

import static org.junit.jupiter.api.Assertions.*;

class ReallyShakyBankingCoreSystemServiceTest {

    ReallyShakyBankingCoreSystemService shaky = new ReallyShakyBankingCoreSystemService();

    @Test
    void transfer() {
        Transaction tx = new Transaction();
        Account from = shaky.getAccount("123");
        Account to = shaky.getAccount("456");
        shaky.transfer(tx, "123", "456");
        //Uferdig kode
    }

    @Test
    void updateAccount() {

    }

    @Test
    void balance() {
        Account a = shaky.getAccount("123");
        BigDecimal balance = new BigDecimal(100);
        BigDecimal expected = new BigDecimal(100);
        a.setBalance(balance);

        assertEquals(a.getBalance(), expected);
    }

    @Test
    void getAccount() {
        String expectedNum = "99345";
        String actualNum = shaky.getAccount("99345").toString();
        assertEquals(expectedNum, actualNum);
    }
}

 */